/*
When the browser context menu is opened (right-click)
- a "Default URL" parent menu item should be visible.
- the menu should contain "Reset current tab to default URL" option.

When the user selects "Reset current tab to default URL" from context menu
- the tab should close and reopen with the default URL.
*/