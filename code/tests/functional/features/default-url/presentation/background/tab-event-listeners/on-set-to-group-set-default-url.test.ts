/*
When a normal tab is moved to a group by the user
- if no default URL exists for that tab, the current URL should be saved as default.
- if a default URL already exists, it should remain unchanged.
*/