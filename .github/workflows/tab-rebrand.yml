name: Release tab-rebrand

on:
  push:
    branches:
      - main
      - beta
    paths:
      - .github/workflows/tab-rebrand.yml
      - code/package.json
      - code/pnpm-lock.yaml
      - code/pnpm-workspace.yaml
      - code/turbo.json
      - code/packages/**
      - code/extensions/tab-rebrand/**

permissions:
  contents: write

jobs:
  release-and-submit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: code
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Check for tab-rebrand changes
        id: check-changes
        run: |
          if [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          elif ! git rev-parse --verify --quiet "${{ github.event.before }}^{commit}"; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          elif git diff --name-only "${{ github.event.before }}" HEAD -- extensions/tab-rebrand/ | grep -q .; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup pnpm
        if: steps.check-changes.outputs.changed == 'true'
        uses: pnpm/action-setup@v4
        with:
          package_json_file: code/package.json

      - name: Setup Node
        if: steps.check-changes.outputs.changed == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: code/pnpm-lock.yaml

      - name: Install dependencies
        if: steps.check-changes.outputs.changed == 'true'
        run: pnpm install --frozen-lockfile

      - name: Zip Extension
        if: steps.check-changes.outputs.changed == 'true'
        run: pnpm --filter tab-rebrand zip

      - name: Get Version
        id: version
        if: steps.check-changes.outputs.changed == 'true'
        run: |
          VERSION=$(jq -r .version extensions/tab-rebrand/package.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create Release
        if: steps.check-changes.outputs.changed == 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: tab-rebrand-v${{ steps.version.outputs.version }}${{ github.ref_name == 'beta' && '-beta' || '' }}
          target_commitish: ${{ github.sha }}
          name: tab-rebrand v${{ steps.version.outputs.version }}${{ github.ref_name == 'beta' && ' (Beta)' || '' }}
          files: code/extensions/tab-rebrand/.output/*-chrome.zip
          generate_release_notes: true
          draft: false
          prerelease: ${{ github.ref_name == 'beta' }}

#      # --- Submission Step (Only runs on main branch and if changes exist) ---
#      - name: Setup Chrome Web Store credentials
#        if: github.ref_name == 'main' && steps.check-changes.outputs.changed == 'true'
#        run: |
#          cat > extensions/tab-rebrand/.env.submit << 'EOF'
#          TAB_REBRAND_CHROME_EXTENSION_ID=${{ vars.TAB_REBRAND_CHROME_EXTENSION_ID }}
#          TAB_REBRAND_CHROME_CLIENT_ID=${{ vars.TAB_REBRAND_CHROME_CLIENT_ID }}
#          TAB_REBRAND_CHROME_CLIENT_SECRET=${{ secrets.TAB_REBRAND_CHROME_CLIENT_SECRET }}
#          TAB_REBRAND_CHROME_REFRESH_TOKEN=${{ secrets.TAB_REBRAND_CHROME_REFRESH_TOKEN }}
#          TAB_REBRAND_CHROME_PUBLISH_TARGET=${{ vars.TAB_REBRAND_CHROME_PUBLISH_TARGET || 'default' }}
#          TAB_REBRAND_CHROME_SKIP_SUBMIT_REVIEW=${{ vars.TAB_REBRAND_CHROME_SKIP_SUBMIT_REVIEW || 'false' }}
#          EOF
#
#      - name: Submit to Chrome Web Store
#        if: github.ref_name == 'main' && steps.check-changes.outputs.changed == 'true'
#        run: pnpm --filter tab-rebrand submit
#